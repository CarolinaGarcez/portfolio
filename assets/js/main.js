document.addEventListener("DOMContentLoaded", () => { const element = document.querySelector("#element"); if (element) { new Typed("#element", { strings: ["Front-End Developer em formação", "HTML, CSS e JavaScript", "Suporte de TI"], startDelay: 1000, typeSpeed: 50, backSpeed: 80, loop: true }); } });; const scrollBtn = document.getElementById("scrollBtn"); scrollBtn.addEventListener("click", () => { const sections = document.querySelectorAll("main section"); const scrollPosition = window.scrollY; for (let i = 0; i < sections.length; i++) { const sectionTop = sections[i].offsetTop; if (sectionTop > scrollPosition + 50) { sections[i].scrollIntoView({ behavior: "smooth" }); break; } } });; const toggleThemeBtn = document.getElementById("toggle-theme"); const toggleIcon = toggleThemeBtn ? toggleThemeBtn.querySelector("i") : null; const currentTheme = localStorage.getItem("theme"); if (currentTheme === "dark") { document.body.classList.add("dark-mode"); if (toggleIcon) { toggleIcon.classList.replace("fa-moon", "fa-sun"); } } if (toggleThemeBtn) { toggleThemeBtn.addEventListener("click", () => { document.body.classList.toggle("dark-mode"); if (document.body.classList.contains("dark-mode")) { if (toggleIcon) toggleIcon.classList.replace("fa-moon", "fa-sun"); localStorage.setItem("theme", "dark"); } else { if (toggleIcon) toggleIcon.classList.replace("fa-sun", "fa-moon"); localStorage.setItem("theme", "light"); } }); } ; const bars = document.querySelector(".bars"); const menu = document.querySelector("#menu"); if (bars && menu) { bars.addEventListener("click", () => { menu.classList.toggle("mobile-menu"); }); };